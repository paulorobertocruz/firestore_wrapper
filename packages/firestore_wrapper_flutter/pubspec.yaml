name: firestore_wrapper_flutter
version: 0.0.2
environment:
    sdk: '>=2.0.0 <3.0.0'
dependencies:
    flutter:
        sdk: flutter
    cloud_firestore: ^0.12.6
    firestore_wrapper:
        git:
            url: https://github.com/paulorobertocruz/firestore_wrapper.git
            path: packages/firestore_wrapper
